{
  "name": "brokensite",
  "version": "1.0.0",
  "description": "AI-powered website diagnostics tool",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "npm ci --prefix frontend && npm run build --prefix frontend"
  }
}
